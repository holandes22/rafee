<div class="container-fluid">
  <div class="row">
    <div class="col-md-2">
      <div class="input-group input-group-sm">
        {{input type="text" value=searchText placeholder="Search..." class="form-control"}}
        <span class="input-group-addon"><span class="glyphicon glyphicon-search"</span></span>
      </div>
    </div>
    <div class="col-md-2">
      {{link-to 'Add' 'slideshows.add' class='btn btn-primary btn-sm' title='Add slideshow'}}
    </div>
  </div>
  <div class="row">
    <div class="col-md-5">
      {{#if model}}
        <table class="table">
          <thead>
            <tr>
              <th>Name</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            {{#each model}}
              <tr>
                <td>{{name}}</td>
                <td>
                  {{#link-to "slideshow.edit" this title="Edit"}}
                    <span class="glyphicon glyphicon-edit"></span>
                  {{/link-to}}
                  {{#link-to "slideshow.delete" this title="Delete"}}
                    <span class="glyphicon glyphicon-remove"></span>
                  {{/link-to}}
                  {{#link-to "slideshow" this title="Details"}}
                    <span class="glyphicon glyphicon-list"></span>
                  {{/link-to}}
                </td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      {{/if}}
    </div>
    <div class="col-md-7 details-pane">
      {{outlet}}
    </div>
  </div>
</div>
